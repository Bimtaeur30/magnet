# Phase 4 — Drawer·Bootstrap 배선

## 목표

`HybridDrawer`/Health·Blame Fill 경로를 `AreaBundleDrawer`로 교체.

## 결과

- `AreaBundleDrawer`
- `BlockSpawnBootstrap` — `areaBundlePoolSO` 주입, Hybrid/Blame 제거
- 씬 Bootstrap 2곳에 `DefaultAreaBundlePool` 연결
