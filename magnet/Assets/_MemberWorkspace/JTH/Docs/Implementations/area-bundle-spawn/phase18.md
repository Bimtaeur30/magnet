# Phase 18 — 직사각 패널티 찬 칸만

## 목표

직사각 greedy 개수를 **찬(occupied) 마스크만** 센다. 빈 칸 분할은 제외.

## 구현 내용

- `CountRectangles` — `PartitionCount(occupiedMask)`만 반환
- Tooltip·튜닝 문서 문구 동기화

## 범위 밖

- size base(empty/filled), Area 개수 패널티, cascade 규칙 변경 없음
