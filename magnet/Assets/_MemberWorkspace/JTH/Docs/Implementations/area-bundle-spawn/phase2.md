# Phase 2 — 42-ID 번들 풀 + Early/Normal Area 최대

## 목표

초반·기본 리스트와 Area 최대 선택기를 붙인다. 유일수 리스트 슬롯은 비워 두고 샘플 대기.

## 결과

- `AreaBundleEntry` / `AreaBundlePoolSO` / `AreaBundleStarterData` (early 20 · normal 40 스타터)
- `AreaBundleOrchestrator.SelectByMaxArea`
- SO `DefaultAreaBundlePool`에 early/normal 채움
